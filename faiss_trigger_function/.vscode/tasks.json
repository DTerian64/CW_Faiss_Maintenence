{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "func: host start",
            "type": "shell",
            "command": "func",
            "args": ["start", "--python-debug", "9091"],
            "isBackground": true,
            "problemMatcher": "$func-python-watch",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}